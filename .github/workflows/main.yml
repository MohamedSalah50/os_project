on:
  push:
    branches:
      - master 





jobs:
  build:
    runs-on: ubuntu-latest

    steps: 
      - name: 'printing folder content(before)'
        run: ls

      - uses: actions/checkout@v3

      - name: 'doker login'
        run: docker login --username mohamed203 --password dckr_pat_tHHmaP0I1ru71b38BNnKMBbUV6g


